Module Module1

    Sub Main()

        Dim l As LinkedList

        l.initialize(5)

        l.insertNewData("e")
        l.insertNewData("d")
        l.insertNewData("c")
        l.insertNewData("b")
        l.insertNewData("a")

        l.printList()

        l.reverseList()

        l.printList()

        'l.traceList()

        'l.printList()

        'l.deleteNode("b")


        'l.printList()

        'l.deleteNode("a")

        'l.printList()

        'l.deleteNode("a")

        'l.deleteNode("e")

        'l.printList()

        'l.traceList()

    End Sub

End Module